#include <iostream>
#include <cstring> //memset
#include <algorithm>
#include <bitset>
#include <string>
#include <vector>
#include <utility>
#include <cstdio>
#include <queue>
#include <cmath>
#include <list>
#include <set>
#include <map>

using namespace std;

int main() {
	freopen("input.txt", "r", stdin);
	int houses, i, bottles, cumulative, sum;
	while (cin >> houses && houses != 0) {
		cumulative = 0; sum = 0;
		for (i = 0; i < houses; i++) {
			cin >> bottles;
			
		}
		cout << sum << endl;
	}
	return 0;
}