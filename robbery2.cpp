#include <iostream>
#include <cstdio>
#include <cstdlib>
int main(void)
{
	int test;
	scanf("%d",&test);
	for (int i=0; i<test; i++)
	{
		int bandits;
		unsigned long long int coins, hissa[bandits];
		scanf("%llu %d", &coins, &bandits);
		for (int j=0; j<bandits; j++)
		{
			hissa[j] = 
		}
	}
	return 0;
}